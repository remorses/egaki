---
fps: 30
bpm: 120
---

import { Dot, ListItem, FeatureGrid } from './components'
import { FEATURES } from './data'
import { AsyncStats } from './async-stats'
import { TextToSpeech } from 'egaki/text-to-speech'
import Tagline from './tagline.mdx'

<Audio src="/minecraft-soundtrack.mp3" />

# Layout A duration=0.7s

<div style={{ display: 'flex', flexDirection: 'row', alignItems: 'baseline', justifyContent: 'center', gap: 30 }}>
  <LayoutTransition id="hero">
    <span style={{ fontSize: 160, fontWeight: 700, color: '#fafafa', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>egaki</span>
  </LayoutTransition>
</div>

# Layout B duration=2s

<div style={{ display: 'flex', flexDirection: 'row', alignItems: 'baseline', justifyContent: 'center', gap: 30 }}>
  <LayoutTransition id="hero" duration={20}>
    <span style={{ fontSize: 160, fontWeight: 700, color: '#fafafa', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>egaki</span>
  </LayoutTransition>
  <TranslateX from={300} to={0} duration={20} inline label="video-slide">
    <span style={{ fontSize: 160, fontWeight: 700, color: '#818cf8', letterSpacing: '-0.04em', fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", sans-serif' }}>video</span>
  </TranslateX>
</div>

# Opening duration=3.3s

<Opacity from={0} to={1} duration={30} label="opening-fade">
  <BlurReveal text="Create videos from code" fontSize={72} />
</Opacity>

# Arrakis duration=6s

<Video src="/arrakis.mp4" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />

# Tagline duration=2.8s

some text

## another duration=2s

<Tagline />

# Create duration=9.5s

<Opacity from={0} to={1} duration={15} label="create-in">
  <Opacity from={1} to={0} duration={15} startInFrames={-15} label="create-out">
    <CodeBlock title="create.sh" language="bash" theme="vercel" width={780}>
{`npx -y egaki create my-video
cd my-video && pnpm dev`}
    </CodeBlock>
  </Opacity>
</Opacity>

# Configure duration=6.7s

<Opacity from={0} to={1} duration={12} label="config-in">
  <Opacity from={1} to={0} duration={15} startInFrames={-15} label="config-out">
    <CodeBlock title="video.mdx" language="mdx" theme="vercel" width={780}>
{`---
fps: 30
---

# Intro duration=3s

<BlurReveal text="Hello" />`}
    </CodeBlock>
  </Opacity>
</Opacity>

# Features duration=5.3s

<Opacity from={0} to={1} duration={12} label="features-in">
  <Opacity from={1} to={0} duration={15} startInFrames={-15} label="features-out">
    <FeatureGrid features={FEATURES} />
  </Opacity>
</Opacity>

# Analytics duration=6s

<Opacity from={0} to={1} duration={15} label="analytics-in">
  <Opacity from={1} to={0} duration={15} startInFrames={-15} label="analytics-out">
    <Server>
      <AsyncStats />
      <TextToSpeech text="Analyticsz that build themselves." />
    </Server>
  </Opacity>
</Opacity>

# Active List duration=6s

<Fill style={{ padding: '0 200px' }}>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 24, width: '100%' }}>
    <ListItem label="Image Generation" description="AI-powered image creation">
      <LayoutTransition id="dot" showFrom={0} showUpTo={2 * FPS} duration={18} bounce={0.3}>
        <Dot />
      </LayoutTransition>
    </ListItem>
    <ListItem label="Video Rendering" description="MDX to MP4 in-browser">
      <LayoutTransition id="dot" showFrom={2 * FPS} showUpTo={4 * FPS} duration={18} bounce={0.3}>
        <Dot />
      </LayoutTransition>
    </ListItem>
    <ListItem label="Voice Synthesis" description="Clone and generate speech">
      <LayoutTransition id="dot" showFrom={4 * FPS} showUpTo={6 * FPS} duration={18} bounce={0.3}>
        <Dot />
      </LayoutTransition>
    </ListItem>
  </div>
</Fill>

# Closing duration=3.3s

<Opacity from={0} to={1} duration={15} label="closing-fade">
  <ShimmerSweep text="egaki" fontSize={88} fontWeight={700} baseColor="#3f3f46" shineColor="#fafafa" />
</Opacity>
